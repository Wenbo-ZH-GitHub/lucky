Transitive Array: An Efficient GEMM Accelerator with Result Reuse
《传递矩阵：一种带有结果复用的有效GEMM加速器》 ISCA2025 MIT 韩松团队

洞察：二进制矩阵中行向量间的包含关系（如行A包含行B的非零位），提出通过结果复用避免重复计算。
优势：相比传统稀疏优化（跳过零值计算），传递稀疏性实现8倍计算量削减，且精度无损。

技术方案：
硬件架构设计——Transitive Array
  乘法消除：将GEMM转化为纯加法操作，通过前缀处理单元（PPE，采用XOR+计算行间差异）和累加单元（APE，整合前缀结果生成输出）实现。
  动态调度机制：采用动态记分牌，在线构建哈斯图（Hasse Graph），解决动态激活（Attention层）的依赖问题；负载均衡，通过轮询调度将节点分配到并行通道，消除依赖。
系统优化设计——高效并行化与调度
  哈斯图分区：将传递关系拆分为独立子树森林，实现无依赖并行。
